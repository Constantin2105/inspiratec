import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const CandidatureValidationCenter = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Validation des Candidatures</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Ce composant est en cours de refonte.</p>
      </CardContent>
    </Card>
  );
};

export default CandidatureValidationCenter;