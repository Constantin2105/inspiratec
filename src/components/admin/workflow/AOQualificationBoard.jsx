import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const AOQualificationBoard = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Qualification des Appels d'Offres</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Ce composant est en cours de refonte.</p>
      </CardContent>
    </Card>
  );
};

export default AOQualificationBoard;